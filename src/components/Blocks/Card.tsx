// Assuming you're using React/Next.js
import React from 'react';
import CardBlock from './CardBlock';

const Card = ({ data }) => {
  return (
    <CardBlock data={data} />
  )
};

export default Card;
